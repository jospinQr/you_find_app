import 'package:flutter/material.dart';

class Userinfoscreen extends StatelessWidget {
  const Userinfoscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Column(
      children: [Text("Inscription reussit")],
    ));
  }
}
